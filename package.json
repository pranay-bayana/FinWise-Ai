{
  "name": "finance-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "check:backend": "node --check backend/src/app.js",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "test": "npm test --prefix backend",
    "audit": "npm audit --audit-level=moderate && node scripts/auditFrontend.mjs && npm audit --prefix backend --audit-level=moderate",
    "verify": "npm run lint && npm run build && npm test && npm run audit",
    "test:e2e": "playwright test -c e2e/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "playwright-core": "^1.61.1"
  }
}
